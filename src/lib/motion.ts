/**
 * Shared signature easing (expo-out). Typed as a fixed-length tuple because
 * framer-motion's `Easing` requires a cubic-bezier tuple, not `number[]`.
 */
export const EASE_EXPO: [number, number, number, number] = [0.16, 1, 0.3, 1];
